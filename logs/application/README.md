# Application-access logs (`application` family)

`app-access.ndjson` — synthetic Tier-1 `orders-portal` access requests. Uses
`client.address` (not `source.ip` — see the address-representation quirk in the
field dictionary). Documentation-safe identifiers only.
See [`../README.md`](../README.md).
